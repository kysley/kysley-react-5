import styled from '@emotion/styled'

const Work = styled.div`
  margin: 0.25em;
`

const WorkName = styled.a`
  font-family: Theinhardt-Bold;
  color: #7e00ff;
  cursor: pointer;
  display: inline;
`

const WorkDesc = styled.span`
  display: inline-block;
`

export {
  Work,
  WorkName,
  WorkDesc,
}
